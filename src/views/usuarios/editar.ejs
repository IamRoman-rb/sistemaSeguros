<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Editar Usuario</h1>
    <form action="/usuarios/actualizar/<%= usuario.id %>" method="POST">
        <label for="nombre">
            <input type="text" id="nombre" name="nombre" value="<%= usuario.nombre %>">
        </label>
        <label for="contraseña">
            <input type="password" id="contraseña" name="contraseña" value="<%= usuario.contraseña %>">
        </label>
        <select name="permiso" id="permiso">
            <option value="<%= usuario.permisos %>" selected><%= permiso.descripcion %></option>
            <% permisos.forEach(p => { %>
                <% if (usuario.permisos != p.id) { %>
                 <option value="<%= p.id %>"><%= p.descripcion %></option>
                <% } %>
            <% }) %>
        </select>
        <label for="dni">DNI
            <input type="text" name="dni" id="dni" value="<%= usuario.dni %>">
        </label>
        <a href="/usuarios">Cancelar</a>
        <button type="submit">Enviar</button>
    </form>
</body>
</html>